# Loops forever: navigate near oak leaves, aim only when a leaf is targeted,
# then hold attack until that targeted leaf breaks before searching again.
start

forever {
  goal_nearest_block leaf 32 4

  repeat_until sensor_targeted_block leaf {
    aim_at_block leaf 32 6t
    wait 1t
  }

  break_block leaf
  wait 3t
}
